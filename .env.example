# Copy this file to `.env` and replace the placeholder with your real connection string.
# Requires PostgreSQL >= 15.
#
# Next.js uses the transaction pooler at runtime.
# Prisma migrations use the session pooler (port 5432) because the transaction
# pooler does not support the prepared statements migrations need.

DATABASE_URL="postgresql://postgres.ekhedkyzztvwvpxboaen:jyC1x13nZmz3yglT@aws-0-ap-southeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.ekhedkyzztvwvpxboaen:jyC1x13nZmz3yglT@aws-0-ap-southeast-2.pooler.supabase.com:5432/postgres"
